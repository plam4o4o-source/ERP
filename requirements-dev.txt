# Инструменти за разработка и проверки (не влизат в продукционния .exe).
# Инсталирай с: pip install -r requirements-dev.txt
-r requirements.txt
pytest>=8.0
bandit>=1.7        # статичен анализ за сигурност (SAST)
pip-audit>=2.7     # сканиране на зависимости за известни уязвимости
playwright>=1.40   # end-to-end тестове с истински браузър (tests/test_e2e_smoke.py, маркер "e2e")
# Одит (12.08.2026, находка №34): само ИНФОРМАТИВНО измерване на покритие
# в CI (виж .github/workflows/ci.yml, стъпка "Coverage report") — без
# твърд праг засега, за да не блокира билда веднага; прави видими
# пропуски като нулево покрити модули, каквито одитът намери ръчно.
pytest-cov>=5.0
