Flask>=3.0
openpyxl>=3.1
# Вграден настолен прозорец (WebView2) само за Windows — на Linux/Mac при
# разработка не се инсталира и не се ползва (програмата пада към браузър).
pywebview>=5.0; sys_platform == "win32"
