Flask>=3.0
openpyxl>=3.1
# Production-grade WSGI сървър за мрежов режим (виж находка M6 —
# вграденият Flask dev сървър не е предназначен за реална продукция:
# еднонишков по подразбиране, без правилно управление на опашка/лимити).
# Ползва се САМО когато мрежов режим е включен в „Системни настройки“ —
# обикновеният самостоятелен режим (127.0.0.1) продължава да ползва
# вградения dev сървър на Flask, както досега (по-лек старт, без нужда
# от допълнителни нишки/процеси за единствения локален потребител).
waitress>=3.0
# Шифроване на GitHub токена в pacho_config.json „в покой“ (виж
# secrets_store.py) — вместо чист текст на диска. Добре одитирана,
# широко използвана библиотека (не самоделна крипто схема).
cryptography>=42.0
# Резервен пакет доверени коренови сертификати — при повредено/непълно
# системно хранилище на Windows (чест проблем с антивирусни програми с
# TLS инспекция), обновяването и GitHub синхронизацията иначе биха
# гърмели с CERTIFICATE_VERIFY_FAILED.
certifi>=2024.0
# Вграден настолен прозорец (WebView2) само за Windows — на Linux/Mac при
# разработка не се инсталира и не се ползва (програмата пада към браузър).
# Виж desktop.py (run_native_window) — при компилирания .exe (sys.frozen)
# ТОВА е основният начин на стартиране, не браузър: Flask сървърът тръгва
# във фонова нишка в СЪЩИЯ процес, а прозорецът е вграден WebView2, без
# изобщо да се отваря отделен браузър процес. Обикновен браузър таб се
# ползва САМО ако pywebview/WebView2 не успеят да се инициализират (виж
# .github/workflows/release.yml — публикуваните бележки на релийза трябва
# да описват точно това, не "отваря браузъра"). Долна граница 6.0 (не 5.0)
# — по-нова стабилна главна версия към момента на писане (проверено в PyPI:
# най-новата налична е 6.2.1); без горна граница, за да се тегли автоматично
# най-новата версия при всяко компилиране (по-добра WebView2 съвместимост
# с нови Windows инсталации с времето).
pywebview>=6.0; sys_platform == "win32"
# Многоезичен интерфейс (БГ/EN/TR) — виж appcore._select_locale() и
# translations/README.md. Не засяга печатните документи (те си остават
# твърдо двуезични БГ/EN по закон, независимо от избрания език).
Flask-Babel>=4.0
# PDF износ на документи (бутон „Изтегли PDF“, виж pdf_export.py) — избрана
# пред weasyprint/wkhtmltopdf нарочно: и двете изискват нативни бинарни
# зависимости (GTK/Cairo/Pango, съотв. отделен wkhtmltopdf.exe), които са
# трудни/крехки за пакетиране в самостоятелен PyInstaller .exe за Windows.
# xhtml2pdf е "чист" Python пакет (тегли reportlab/Pillow/html5lib/pypdf —
# всичките чист Python или широко тествани с PyInstaller колела).
xhtml2pdf>=0.2.17
# Растеризация на Code128 баркода за PDF износа (pdf_export.py през
# barcode128.code128_png_data_uri) — xhtml2pdf не рисува вложен <svg>
# маркъп като истинска векторна графика, само PNG/JPEG изображения. Вече е
# транзитивна зависимост на reportlab/xhtml2pdf, но е декларирана изрично
# тук, защото barcode128.py я импортира директно.
Pillow>=10.0
